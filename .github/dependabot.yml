version: 2
updates:
  - package-ecosystem: "github-actions"
    # "/" covers every workflow file under .github/workflows/.
    directory: "/"
    schedule:
      interval: "weekly"
    groups:
      actions:
        patterns:
          - "*"
