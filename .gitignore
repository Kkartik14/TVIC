node_modules
.pnpm-store/
dist
.turbo
.next
*.tsbuildinfo
.DS_Store
.env
.env.local
.env.*.local
coverage
.vercel
*.log

# Private research and internal working notes. Keep these local; do not commit them.
docs/
books/
CONTEXT.md

# Per-directory agent memory notes. Local orientation only; never commit them.
.memory/

# Local planning/PM working docs (PRDs, action-item breakdowns). Never commit them.
local/
