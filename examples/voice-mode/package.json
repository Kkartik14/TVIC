{
  "name": "@tvic/example-voice-mode",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "tsx src/main.ts",
    "mint-token": "tsx src/token.ts",
    "test": "node --check public/voice-client.js && node --check public/pcm-worklet.js && vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit && tsc -p tsconfig.test.json"
  },
  "dependencies": {
    "@tvic/core": "workspace:*",
    "@tvic/dal": "workspace:*",
    "@tvic/providers": "workspace:*",
    "@tvic/runtime": "workspace:*"
  },
  "devDependencies": {
    "@types/ws": "^8.18.1",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "ws": "^8.21.0"
  }
}
